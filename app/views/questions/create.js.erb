<% unless @question.errors.present? %>
  $("#questions").append("<%= escape_javascript(render @question) %>");
<% end %>
$('.flash-wrapper').html("<%= escape_javascript(render 'layouts/messages') %>");